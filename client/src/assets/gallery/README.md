Place reusable source-managed gallery assets here when they need to be imported by React components.

The live workshop gallery currently loads replaceable public images from `client/public/gallery` so non-developers can update photos without touching component imports.
